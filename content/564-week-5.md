https://cwe.mitre.org/data/definitions/416.html

weaknesses !== vulnerabilities


rowhammer
- cache line flush 
heartbleed



buffer overflows
stack smashing
- ![[Pasted image 20260302175918.png]]
- NOP sled
- modern version:
	- stack alignment
	- 64-bit code > bigger offsets, all addresses may have zeroes in them, calling conventions pass via registers instead of stack
	- 
ROP 
- how programs built based upon the SP instead of the PC ? 
- ROP instruction = word on stack points to instruction sequence ending in RET instruction,
- execution sequence: 
	- set PC to word pointed to by SP 
	- needs PC writeable or absolute jumps (jump instruction goes to absolute location)
	- incment SP
	- PC executes until RET
	  continue tfor next SP
- NOP gadget: 
	- set immediate gadeget: push register value on stack
- ROP operationally: need stack control for write to FP, SP, 
	- depending on target's program 
	- avoid write for execute 

- ROP gadget in x86 has an advantage over arm 
	- instructions align


sigreturn oriented programming
- instead of return opcodes, returns from signal handlers
- kernel delivers signal
- user space execution switched to signal handler context
- frame used to guide back to normal execution once handler finished
- SROP vs signal exection return ROP 


can ROP be done wiht position of independent code ?

can you get gadgets from the binary code itself? 


microarchitectural attacks
- timing -> potential information leaks, side-channel attacks
- you cannot code secure on an insecure architecture
- caches
- pipelines: instructions clock cycle

attacking caches: probing
- caches line not flushed on context switch 
	- special case: L2/L3 cache -shared between processes adn cores
	- user can sometimes specifically target a cache line via clflush on x86


flush + reload 
- x86 specific : 
- flush targeted cach lines deliberately
- determine time to reload: how long to access victim cache adn what lines change
- how long attackers to access lines after victim ran
- attackers use this timing attack to determin if info needed to be reloaded

flush monitored line from memory
wait for victim to access memory
reload line via attacker measuring access time



attacking pipeline: 



- speculation attacks
- branch predictor attack
- TLB attack
- intel/AMD microop caches


