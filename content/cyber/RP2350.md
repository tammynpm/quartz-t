---
title: Untitled 6
tags: []
draft: true
date: 2026-04-29
---
overview: 
- antifuse based OTP memory serves as the root of trust of the system -> informs the ocnfig of arm trustzone as well as additional attack mitigations like glitch detectors

[datasheet](https://pip-assets.raspberrypi.com/categories/1214-rp2350/documents/RP-008373-DS-2-rp2350-datasheet.pdf?disposition=inline)


prereqs: 
- SRAM 
- ROM code 
- OTP
- 

ROM: read only memory 
- canno tbe patched, vulnerabilities are permanent -> attackers can run unauthorized code before the OS secure the hardware. 
boot ROM : a part of ROM used for booting system, 
- contains instructions run **after** the CPU is reset to the reset vecotr
- typically loads a bootloader 
- 2 types:
	- mask boot ROM cannot be changed afterwards 
	- writable boot ROM like EEPROM or flash memory
-> gain high privilege control, jailbreak devices


sram -> cpu caches, high speed registers 
ram -> main memory, higher density, lower cost per bit


## rp2350 
can be booted in 2 modes; arm, risc-v 



## OTP one-time programmable 

OTP guard read mechanism - reads of known data (0x333333) -> really close to reads of security-critical data 0> a powre fault cause a mismatch in guard ata 
-> ROM defense against power-glitch attack 


optical reverse engineering -> OTP block is powered separately on a pin called USB_OTP_VDD -> if power to the OTP is dropped immediately after the guard value is read -> system continues returning 0x333333 even for subsequent security-critical reads -> make th chip think it read all zeros for its security configuration 



### OTP boot sequence: 
PSM Power sequencing machine -> hardware state machine that reads security config from antifuse OTP everytime the chip resets 

1. run 'synopsys' boot instruction 
2. read CRIT0, CRIT1
3. read HW keys > cryptographic keys for secure boot signature verification
4. read KEY_VALID -> flags indicating which keys are actually programmed
5. read page locks -> access control settings for each OTP page

#### crit1
a rgister
can only be programmed once 
controls:
- debug disable: 
- secure boot
- boot architecture
- glitch detector

#### antifuse OTP
- highly secure non-volatile memory (NVM) -> creats a permanent, nonreversible connection by breaking donw 
- use case: secure key storage, analog trimming, confi

how it works:
- create a permanent connection using a high voltage to 

rows in OTP 
![[Pasted image 20260429160048.png]]

![[Pasted image 20260429160212.png|697]]


guarded reads - 

glitch detector 

antifuse fuse club 

![[Pasted image 20260429160257.png|697]]

4 magic words provided by synopsis 





escalate from risc-v -> M33 secure-mode debug 



![[Pasted image 20260429161903.png|697]]



### bootstrap problem 

chip neesd to read security ocnfig before it enforces security --> creates  a brief window duering power-on where the chip is completely unprotected reading OTP to figure out how to protect itself -> glitch attack 

guard read mechanism was supposed to solve this: verify the OTP is returning valid data before trusting the security bits
but synopsys OTP block retains teh last-read value when poewr drops -> guard value 0x333333 persists into the crit0/critt1 reads -> guard check passes even though teh real data was never read. 

glitch detectors cannot help because their own enable bit is **inside crit1 the register being corrupted -> detector cannot protect the read that activates it**

one 50 $\mu s$ voltage drop on 1 pin collapses entire stack b/c every protection depends on teh same root: a correct otp read at boot 




ther emight be different power for differnet part of the chip like the I/O, analog/digital supply 


mabye can use EM side channle if he doesn't have the power, or the SPA telling me that this is changing the behavior 


uv attacks use case: floating gate, nvram, prom, eprom, eeprom, flags 
antifuse isn't susceptible to uv attacks 


what kind of systems 